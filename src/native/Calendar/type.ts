/**
 * calendar type
 */
export type { Calendar } from 'react-native-calendar-events'

export enum AuthStatusEnum {
  denied = 'denied',
  restricted = 'restricted',
  authorized = 'authorized',
  undetermined = 'undetermined'
}
